package com.calcite_new.sql.model.enums;

/**
 * Enum representing different SQL clause types where columns can be used.
 */
public enum ClauseType {
	WHERE,
	GROUP_BY,
	JOIN,
	HAVING,
	ORDER_BY,
	SELECT
}

