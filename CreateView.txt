Subject: BigQuery Cost-Optimization Findings – Partition & Query Efficiency Improvements

Hi <Client Name>,

As part of our ongoing review to identify BigQuery cost-reduction opportunities, we analyzed high-scanning queries in the development and production environments. Below are two key findings that highlight immediate areas where cost and performance can be improved without changing any business logic or output.

1. Missing Partition Filter in PAYMENT_MART_PAYMENT_MI Query

During review of the PAYMENT_MART_PAYMENT_MI process, we identified a query that was scanning approximately 3.5 GB of data. The query was not filtering on the table’s partition column, which caused unnecessary full-table scans.

After adding the appropriate partition filter, the query produced the same result while reducing the scanned volume to under 1 GB.
This directly lowers recurring on-demand processing cost and improves runtime.

(Screenshots attached – before & after scan size)

2. Demonstration of Cost Benefit: Partitioned vs Non-Partitioned Tables

In production, there is a daily delete operation that scans 8 TB+ each run because the table is not partitioned on the load_datetime column—despite this column being used as the filter in the delete condition.

To demonstrate the optimization potential, we created two sample tables (~30 GB each) in the development environment:

es2 (non-partitioned) – deleting with date filter scanned the entire ~30 GB

es1 (partitioned on the date column) – the same delete condition scanned 0 bytes

Both tables contain identical data; the only difference is partitioning.
This clearly shows the potential cost reduction if production tables adopt a similar partitioning strategy.

(Screenshots attached – es1 vs es2 scan details)

These findings illustrate the tangible cost benefits of ensuring queries use partition columns and of aligning table structure (e.g., partitioning on load_datetime) with how queries filter data. Applying these optimizations to production will significantly reduce scan volumes and recurring processing cost.

I’ve attached all supporting screenshots, including the production query reference.

Regards,
<Your Name>
